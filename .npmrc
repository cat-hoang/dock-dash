registry=https://registry.npmjs.org/

# Ensure platform-specific optional deps (e.g. @rollup/*) are resolved
# for both local dev (Windows) and CI/Docker (Linux).
os[]=current
os[]=linux
cpu[]=current
cpu[]=x64
cpu[]=arm64
